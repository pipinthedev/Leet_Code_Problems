const os = require('os');


console.log(os.arch());
console.log(os.cpus());
console.log(os.uptime());
console.log(os.totalmem());
// there are many functions under os. which we need to study more! probably will use it in next project or smth to make it work with js !

// os is used to know the client infomation to get some values and valuable information from the server and do some operations that match witht he client system configuration